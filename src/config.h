#pragma once

#include <QtGlobal>

const qreal CFG_ROOM_TMPR = 86.0; //in F
const qreal CFG_MOCK_HEAT_SPEED = 50; // F/minute
const qreal CFG_MOCK_COOL_SPEED = 20; // F/minute
const qreal CFG_MOCK_HEAT_MIN_DIFF = 1; // min diff to trigger heater
const qreal CFG_MOCK_TIME_FACTOR = 20;
const qreal CFG_MOCK_CAMERA_TRAY_ROTATION_SPEED = 30;
